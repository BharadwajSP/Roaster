
<%= form_for(@shift_detail) do |f| %>
  <% if @shift_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shift_detail.errors.count, "error") %> prohibited this shift_detail from being saved:</h2>

      <ul>
      <% @shift_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">

    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time, class: 'form-control' %><br>

    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time, class: 'form-control' %><br>

    <%= f.label :description %><br>
    <%= f.text_area :description, class: 'form-control' %><br>

  <% if current_user.email == "keshav.sp@tcs.com" %>
    <%= f.label :user_id %> <br>
    <%= f.text_field :user_id, class: 'form-control' %><br>
   <% end %>
      <%= f.submit  "Create Shift", class: "btn btn-primary" %>

<% end %>
</div>
</div>
